name: assignment3
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 📷 Выбор изображений (обновленная версия)
  image_picker: ^1.0.4

  # 📍 Определение геолокации
  geolocator: ^10.1.0

  # 🗺️ Карты Google
  google_maps_flutter: ^2.5.0

  # 📂 Доступ к локальному хранилищу
  path_provider: ^2.1.2

  # 📦 База данных (SQLite, если Firebase не используется)
  sqflite: ^2.3.2

  # 📡 Управление состоянием
  flutter_riverpod: ^2.4.9

  # 🖋️ Шрифты Google
  google_fonts: ^6.1.0

  # 🌍 Геокодинг (конвертация координат в адрес)
  geocoding: ^2.2.0

  # 🔑 Запрос разрешений на камеру/геолокацию
  permission_handler: ^11.0.1

  # 🎯 UI-иконки в стиле iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
